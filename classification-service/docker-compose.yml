services:
  app:
    build: .
    ports:
      - "5000:5000"
    shm_size: '1gb'
    environment:
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - DB_PORT=${DB_PORT}
      - CLOUDFRONT_URL=${CLOUDFRONT_URL}